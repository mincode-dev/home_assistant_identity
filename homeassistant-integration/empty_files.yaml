# These are the required empty files for Home Assistant configuration
# Copy these to your HA config directory if they don't exist

# scripts.yaml (empty file required for !include)
# Content: (empty)

# scenes.yaml (empty file required for !include) 
# Content: (empty)

# Note: automations.yaml is created dynamically by users through the HA UI
# so it should not be tracked in this project